# Приёмка и демо: умный подбор подрядчиков

Подготовлено с помощью Codex в рамках роли проверки качества. Базовая проверенная версия: `df173a9` (23.09.2026). Источники требований: `docs/TASK.md`, `docs/CASE_SPEC_RU.md`, `docs/RUBRIC.md`. Этот документ фиксирует проверку прототипа, а не гарантирует готовность будущих версий.

## Воспроизводимые сценарии

Запустить сервер из корня проекта после установки зависимостей:

```powershell
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --port 8000
```

В другом PowerShell выполнить базовый запрос (используется UTF-8):

```powershell
$query = @{
    city = 'Алматы'
    category = 'Ведущий'
    event_type = 'свадьба'
    date = '2026-10-15'
    budget_kzt = 1000000
}
$body = [System.Text.Encoding]::UTF8.GetBytes(($query | ConvertTo-Json))
$result = Invoke-RestMethod -Uri 'http://127.0.0.1:8000/match' -Method Post -ContentType 'application/json; charset=utf-8' -Body $body
$result | ConvertTo-Json -Depth 10
```

Для каждого следующего сценария менять поля `$query`, заново вычислять `$body` и отправлять запрос. Каждый сценарий ниже начинается с базового запроса; изменения не накапливаются.

| Сценарий | Изменение относительно базового | Наблюдалось в df173a9 |
|---|---|---|
| Плотная категория | Нет | `found`, 3 карточки: HK-35215, HK-42352, HK-27222 |
| Редкая категория | category = Флорист | `found`, 2 карточки: HK-90001, HK-39372; объяснено сокращение выдачи |
| Никто не проходит условия | budget_kzt = 0 | `no_match`, 0 карточек, текст причины |
| Вторая дата | date = 2026-10-16 | `found`, 2 карточки: HK-77838, HK-44923 |
| Детерминизм | Повторить базовый запрос трижды | Порядок ID должен оставаться одинаковым |

Дополнительно проверить третий исход `no_category_in_city`: взять сочетание города и категории, отсутствующее в каталоге; сверить с `/options` и CSV. Не выдавать технически несуществующее название категории за реальный пользовательский сценарий.

Приведённые ID — наблюдения для конкретного коммита и официального CSV, не данные для подстановки в приложение. После изменения ранжирования повторно проверить и обновить наблюдения.

## Результат первого прогона

- 40 тестов прошли (`python -m pytest -q -p no:cacheprovider`), 1 предупреждение устаревающего alias в зависимости Starlette/AnyIO. Python 3.12, зависимости из requirements.txt установлены отдельно для ревью.
- Сервер реально запущен через uvicorn; `/health`: status ok, 66 профилей. Главная страница: HTTP 200.
- Выполнены HTTP-запросы плотной/редкой категорий, нулевого бюджета, второй даты и дат вне покрытия. Локальные ответы заняли примерно 3–96 мс; это не нагрузочное тестирование и не измерение LLM.
- CSV репозитория побайтно совпал со скачанным официальным CSV.
- Полное браузерное прохождение формы и чистая установка строго по README пока не выполнены.

## Открытые проблемы версии df173a9

1. **README:** остаются шаблонные поля, нет воспроизводимых шагов демо и описания офлайн-режима. Запуск по README — условие допуска согласно правилам, указанным в docs/HACKATHON_RULES.md. Заполнить до сдачи и проверить на чистом окружении.
2. **Календарь:** официальный диапазон 2026-09-23…2026-12-31. Запросы на 2027-01-01 и 2026-01-01 возвращают карточки с утверждением «по календарю свободен», хотя покрытия нет. Предлагается проверять границы на сервере и в форме; добавить тесты обеих границ и дней за пределами. До исправления не демонстрировать такие ответы как достоверные.
3. **Описание AI:** app/explain.py сейчас склеивает факты в шаблон; LLM — TODO. Документация должна различать реализованный режим и план. Нельзя представлять шаблон как выполненный вызов модели. Сам кейс разрешает LLM, но не обязывает использовать конкретного провайдера.

## Чек-лист перед сдачей

- [ ] Все обязательные поля есть; ошибочные значения дают понятную ошибку.
- [ ] Не более трёх карточек, в каждой имя, категория, город, цена и конкретное объяснение.
- [ ] Занятые подрядчики и площадки исключаются; проверены бюджет, формат, язык и часы.
- [ ] Меньше трёх результатов и все три исхода объясняются словами.
- [ ] Одинаковый запрос сохраняет порядок, две даты дают подтверждённое календарём различие.
- [ ] Объяснения без имён различимы по полезным фактам, а не только по формальной уникальности текста.
- [ ] Диапазон календаря проверен на сервере и в форме.
- [ ] Синтетические и восстановленные поля отмечены; официальные синтетические записи не называются собственными добавлениями команды.
- [ ] Запуск по README проверен без личного API-аккаунта; указаны источники и ограничения.
- [ ] Если добавлен LLM: проверены недоступность API, таймаут, неверный ответ, fallback, отсутствие выдуманных фактов, общее время до 10 секунд.
- [ ] Проверены тесты, запуск, rules-check и видимость часового результата на GitHub.

## Ценность и бизнес-гипотеза

Пользователь — клиент, выбирающий подрядчика на мероприятие. Потенциальный покупатель — агрегатор или ивент-агентство. Польза: короткий список с проверяемыми причинами выбора и отказа вместо ручного просмотра каталога.

Гипотеза монетизации: B2B-подписка или плата за использование модуля подбора. Спрос и готовность платить ещё не проверены. Комиссия с бронирования не подтверждена текущим MVP: бронирований и атрибуции сделок нет.

Метрики пилота: время до короткого списка, предпочтение рекомендаций по сравнению с обычными фильтрами, доля ошибок ограничений и фактов, переходы к контакту. Для себестоимости измерять запросы, расходы API, хостинг и обновление каталога; не заявлять выручку или маржу без данных.

Ограничения: цена «от» не равна окончательной смете; календарю требуется регулярное обновление; анонимизированный и частично синтетический набор подтверждает работу прототипа, но не рыночный спрос.

## Повторная приёмка 24248cd — 23.09.2026

Проверено Codex по поручению Nurdaulet. Старые результаты выше относятся к df173a9; ниже — отдельный прогон актуального снимка 24248cd. Код приложения, README и тесты не изменялись.

### Установка и окружение

Проверялся чистый архив git-коммита без .env и прежних зависимостей, а не сетевой clone: доступ Git из приложения ограничен. Windows, PowerShell. Переменные OPENAI_API_KEY и OPENAI_MODEL очищены только для процесса проверки; системные настройки ключа не менялись.

1. Системная команда `python` здесь означает **Python 3.8.2**, ниже заявленного минимума 3.9. Создание окружения возможно, но установка requirements.txt остановилась на uvicorn==0.34.2. Это несоответствие окружения требованиям, а не доказательство поломки проекта на поддерживаемом Python.
2. `python3` в этом PowerShell отсутствует. README предлагает этот вызов в блоке «Без make (например, на Windows)», далее использует `.venv/bin/python`; Windows-путь дан только комментарием. Для новичка инструкции не копируются буквально. Нужен отдельный блок PowerShell с предварительной проверкой версии Python.
3. С доступным Python **3.12** создано новое окружение `.venv312`, установка requirements.txt успешна. Запуск через `.venv312\Scripts\python -m uvicorn app.main:app --host 127.0.0.1 --port 8767` успешен. Порт изменён для изоляции от возможного пользовательского сервера. Таким образом, подтверждён запуск после выбора поддерживаемого Python и адаптации путей, а не буквальная Windows-инструкция README.
4. **93 passed, 1 warning in 0.87 s** (`python -m pytest -q -p no:cacheprovider`). Warning — устаревающий alias BlockingPortal в зависимости Starlette/AnyIO. README пока пишет 77 тестов.

### Браузер: ввёл → получил → что непонятно

Во всех строках формат «свадьба», язык и часы не заданы. Пять сценариев выполнены именно через форму браузера, без API-ключа. Все показанные карточки отмечены «шаблон».

| № | Ввёл | Получил | Что непонятно / качество |
|---|---|---|---|
| 1 | Алматы, 15.10.2026, Ведущий, 1 000 000 ₸ | 3 карточки: Кики, Эмилия, Сон Гоку; соответствует README | Объяснения различаются, но цитата Кики обрывается на «праздник в честь…» и перечисляет также не относящиеся к свадьбе события. Лучше выбрать короткую завершённую релевантную деталь. |
| 2 | То же, дата 16.10.2026 | Хаул и Мицури Канроджи; 5 заняты, 2 не берут формат, 1 выше бюджета. В раскрытом trace Кики, Эмилия и Сон Гоку заняты именно на эту дату | У Хаула незавершённая фраза «Профессиональный ведущий с интеллигентной». У Мицури вторая цитата — «Приветствую всех, дорогие друзья», не помогающая выбору. Факт более низкой цены при этом полезен. |
| 3 | Алматы, 15.10.2026, Флорист, 500 000 ₸ | Тихиро Огино и Тони Тони Чоппер; «всего 2 профиля», синтетическая запись и восстановленная цена отмечены | Сценарий понятен; цитата второй карточки обрывается на «мероприятий в…». |
| 4 | Астана, 15.10.2026, Декоратор, 1 000 000 ₸ | Нет категории в городе; предложен Алматы | Одинаковое сообщение показано и заголовком, и абзацем — лишний повтор. |
| 5 | Алматы, 15.10.2026, Декоратор, 1 000 000 ₸ | 0 карточек; все 3 кандидата выше бюджета | Понятный пустой исход; основной текст и строка «Подходят 0 из 3» частично дублируются. |

Базовый HTTP-запрос дополнительно повторён трижды: порядок HK-35215, HK-42352, HK-27222 совпал. Это проверка детерминизма выдачи; не оценка качества живой модели.

### Статус предыдущих замечаний

- **README — существенно исправлен, но не полностью.** Основные разделы и пять корректных демо-сценариев появились. Остались Windows-команды, устаревшее число тестов и заполнители участников. rules-check: 0 ERROR, 1 WARN — 3 строки с заполнителями. Раздел доступа также обещает AI после указания ключа, хотя код требует и непустой OPENAI_MODEL; это стоит явно написать в том же абзаце.
- **Календарь — ещё не исправлен в 24248cd.** Через браузер дата 2027-01-01 допустима и возвращает 3 карточки. Через API 2026-09-22 и 2027-01-01 также дают HTTP 200 и 3 карточки. В matched_facts всё ещё записывается доступность вне покрытия. Обе допустимые границы 2026-09-23 и 2026-12-31 дают HTTP 200. Разработчик сообщил, что готовит исправление; этот прогон его не включает.
- **LLM — реализация появилась.** Есть отдельный модуль вызова, проверка структуры, лексический валидатор и fallback. Без ключа реально работает весь демо-сценарий. 93 офлайн-теста прошли. Живой OpenAI-вызов, скорость 2–2.6 с и общая гарантия ответа <10 с в AI-режиме независимо не проверялись; это пока сообщение разработчика. Лексическая проверка не доказывает отсутствие любых смысловых галлюцинаций.

### Что передать разработчику

Приоритет 1: закончить серверную и браузерную проверку диапазона календаря и прислать commit hash. Приоритет 2: дать отдельные команды PowerShell для Python 3.9+, заполнить авторов, обновить число тестов и явные требования ключ+модель. Далее улучшить шаблонные цитаты: не обрывать предложения на зависимом слове, исключать приветствия и нерелевантные события; при отсутствии полезной детали оставить подтверждённые условия цены/формата. Убрать повтор сообщения отсутствующей категории. Файлы app/, web/, tests/, README остаются у разработчика.

**Вердикт:** пять сценариев офлайн-демо проходят; готовность окончательной сдачи не подтверждена до исправления календаря и завершения инструкции запуска. Следующий прогон должен ссылаться на новый commit hash.

## Повторная приёмка 5b44e64 — 23.09.2026

Проверено Codex по поручению Nurdaulet после получения изменений 187382d и 5b44e64. Данный раздел — новое дополнение для согласования; предыдущие результаты сохранены как история проверки.

### Результат и границы проверки

- **103 passed, 1 warning in 1.23 s.** Предупреждение Starlette/AnyIO о BlockingPortal не изменилось. Запущен uvicorn и выполнены браузерные и прямые HTTP-проверки без OpenAI-ключа и без модели; .env не использовался.
- Зависимости установлены из requirements.txt в новое окружение `.venv` на Python 3.12. Проверен чистый снимок git-коммита 5b44e64 без ранее установленных библиотек, а не исходная рабочая папка.
- **Строгая проверка «чистый clone → команды README» ещё не завершена.** Попытка локального git clone заблокирована проверкой владельца репозитория в среде приложения; настройки доверия Git не изменялись. Использован `git archive` как явно обозначенная замена. Команда README `py -3.12 --version` сообщает, что runtime не найден: установленный Windows Launcher видит только 3.8. Для этого прогона окружение создано явным путём к доступному Python 3.12 среды Codex. Дальнейшие команды `.venv\Scripts\python -m pip install -r requirements.txt` и запуска uvicorn работают; тестовый порт 8768 выбран для изоляции.
- Поэтому проблему прежних Windows-путей README считаем исправленной по содержимому; прохождение инструкции на машине с установленным Python 3.12 через Launcher ещё нужно подтвердить. Не следует выдавать успешный запуск альтернативным интерпретатором за буквальное прохождение README.

### Пять сценариев README — повторно в браузере

Формат свадьба, без часов и языка, везде пометка «шаблон». Результаты соответствуют README.

| Ввёл | Получил | Замечания |
|---|---|---|
| Алматы, 15.10.2026, Ведущий, 1 000 000 ₸ | Кики, Эмилия, Сон Гоку | 3 карточки, ожидаемый порядок. Старые замечания к оборванным цитатам остаются. |
| Те же поля, 16.10.2026 | Хаул, Мицури Канроджи | 2 карточки. В раскрытом trace Кики, Эмилия и Сон Гоку заняты на 16.10.2026; сообщение объясняет сокращение выдачи. |
| Алматы, 15.10.2026, Флорист, 500 000 ₸ | Тихиро Огино, Тони Тони Чоппер | 2 из 2 кандидатов; синтетический профиль и восстановленная цена отмечены. |
| Астана, 15.10.2026, Декоратор, 1 000 000 ₸ | Нет категории, указано наличие в Алматы | Текст по-прежнему повторён в заголовке и абзаце. |
| Алматы, 15.10.2026, Декоратор, 1 000 000 ₸ | Нет результатов: у всех 3 цена выше бюджета | Пустой результат объяснён; не ошибка и не пустой экран. |

### Календарь — дефект исправлен

Проверены ограничения формы, её фактическое поведение и отдельный прямой доступ к API.

| Дата | Форма браузера | POST /match |
|---|---|---|
| 2026-09-22 | valid=false; «Минимальное значение должно быть 23.09.2026.» | 422, русское сообщение диапазона |
| 2026-09-23 | valid=true; 3 карточки: Эмилия, Сон Гоку, Мицури Канроджи | 200 |
| 2026-12-31 | valid=true; 2 карточки: Эмилия, Мицури Канроджи | 200 |
| 2027-01-01 | valid=false; «Максимальное значение должно быть 31.12.2026.» | 422, русское сообщение диапазона |
| 2026-01-01 | Дополнительная проверка API | 422, русское сообщение диапазона |

Сообщение API: «Календарь занятости покрывает только 23.09.2026–31.12.2026; выберите дату в этом диапазоне.» Поле date в форме имеет min=2026-09-23 и max=2026-12-31. Даты вне диапазона больше не принимаются прямым запросом в обход формы. Базовый запрос 15.10.2026 повторён три раза через API: порядок HK-35215, HK-42352, HK-27222 неизменен.

### Статус остальных замечаний

- README: число тестов обновлено до 103; участники заполнены; явно требуются и OPENAI_API_KEY, и OPENAI_MODEL; отдельный PowerShell-блок и проверка версии добавлены. `rules-check.sh --stage push`: без ошибок и предупреждений на момент проверки.
- Неудачные шаблонные цитаты и повтор сообщения отсутствующей категории ещё воспроизводятся. Разработчик принял их в работу; текущая версия не содержит этих исправлений.
- Живой LLM-режим, его скорость и полная семантическая достоверность независимым прогоном по-прежнему не проверены. Офлайн-тесты не заменяют такую проверку.

**Вердикт:** календарный дефект закрыт, пять сценариев офлайн-демо и 103 теста проходят. Следующий обязательный шаг проверки воспроизводимости — чистое клонирование и буквальное выполнение README на Windows с установленным Python 3.12. Новых изменений в app/, web/, tests/ и README проверяющий не вносил.

## Приёмка 9f06fc8 — 23.09.2026

Разделы приёмки 5b44e64 и 9f06fc8 согласованы разработчиком через Nurdaulet перед публикацией. Результаты ниже относятся к этим версиям, а не автоматически к последующим коммитам.

- **159 passed, 1 warning in 1.68 s**: офлайн-тесты прошли; предупреждение Starlette/AnyIO о BlockingPortal. Использовано проверенное окружение Python 3.12; requirements.txt не менялся. Это не новая чистая установка.
- Сервер реально запущен; браузерные проверки выполнены без OpenAI-ключа и модели. «Пример» заполняет текст, но сам не отправляет запрос и не меняет поля формы.
- Базовый запрос Алматы / Ведущий / свадьба / 15.10.2026 / 1 000 000 ₸ возвращает Кики, Эмилию, Сон Гоку. У Кики вместо оборванного перечисления цитата «свадьбы», у Эмилии указан опыт 13 лет.
- Той / казахский / 4 часа при остальных базовых параметрах возвращает Кики, Сон Гоку, Эмилию. Во всех объяснениях подтверждены язык и часы: запрошенные 4 ч сравниваются с доступными 10 ч.
- На 16.10.2026 для свадьбы возвращаются Хаул и Мицури Канроджи. Приветствие Мицури и обрыв «с интеллигентной» у Хаула устранены в проверенных сценариях. Выдача проверена также без необязательной длительности.
- Новые rank_reason передают основания начисления баллов в объяснения; добавлены фильтрация цитат и проверка упоминания языка. Прошедшие тесты используют подставленные ответы модели; живой OpenAI-разбор и объяснения, их скорость и полная семантическая точность не проверены.

**Закрыто в проверенных сценариях:** прежние конкретные оборванные цитаты и приветствие, отсутствие явного подтверждения выбранного языка и часов в шаблонах. Повтор «нет категории» был устранён в 37b7eb4. Это не гарантия качества каждого возможного сочетания каталога.

**Открыто на момент 9f06fc8:** README указывает 103 теста вместо 159 и не описывает свободный ввод; обработчик /parse одинаково сообщает о необходимости ключа при отсутствии настроек и при таймауте. Ошибка таймаута воспроизведена офлайн на d5510dd с подставленным клиентом и заданными тестовыми настройками; соответствующая логика в 9f06fc8 не менялась. Разработчик впоследствии сообщил о готовящемся исправлении; оно требует отдельной проверки нового commit hash.

**Вердикт:** новых блокирующих дефектов в проверенном офлайн-сценарии не найдено. Полная проверка воспроизводимости «чистый clone → README» и живой OpenAI-режим остаются отдельными незавершёнными этапами. Проверяющий не менял код приложения.

## Приёмка 2a45fc7 — чистая Windows-установка и живой OpenAI

Дата: 23.09.2026. Проверенная версия: `2a45fc76521e4de58cd2ebc196c71af4fbbee452`.
Ревью по критериям жюри отложено по просьбе участника. Новый дизайн проверен отдельно ниже; эти измерения относятся к 2a45fc7.

### Чистая установка

Выполнен настоящий `git clone` с GitHub в новую папку `work/clean-install-2a45fc7`, затем checkout согласованного коммита. Старое виртуальное окружение и локальные файлы проекта не копировались.

`py --list` показал только Python 3.8, `python --version` — 3.8.2. Использован вариант C из README с реальным полным путём к Python из среды Codex:

```powershell
& '<полный путь к установленному Python 3.12>\python.exe' -m venv .venv
.venv\Scripts\python --version
.venv\Scripts\python -m pip install -r requirements.txt
.venv\Scripts\python -m uvicorn app.main:app --port 8000
```

Результат: Python 3.12.14, зависимости установлены, сервер запущен, страница http://localhost:8000 открылась. `pip check`: No broken requirements found. Это проверка чистого clone и нового venv на существующей Windows, не на новой виртуальной машине.

### Офлайн-проверка

- `.venv\Scripts\python -m pytest -q`: **201 passed**, 2.94 с. Одно предупреждение DeprecationWarning из зависимости Starlette/AnyIO.
- В браузере пройдены все пять сценариев README: Кики/Эмилия/Сон Гоку; на другой дате Хаул/Мицури Канроджи; два флориста с причиной; отсутствие декораторов в Астане; три декоратора выше бюджета в Алматы. Карточки помечены как шаблонные.
- «Пример → Разобрать» без конфигурации показывает сообщение о необходимости ключа и модели, ручной подбор доступен.
- Демо с явным UTF-8: **28/28**, включая календарные границы, детерминизм и скорость. Максимум в этом запуске около 0.010 с.

#### Замечание: UnicodeEncodeError в Windows

Точная команда `.venv\Scripts\python scripts\demo.py` в автоматическом запуске с перехватом stdout падает в `scripts/demo.py:29`:

```text
UnicodeEncodeError: 'charmap' codec can't encode character '\u2705' in position 0: character maps to <undefined>
```

Причина: stdout Python использует CP1251, не поддерживающую ✅/❌. Это подтверждено для запуска с перехватом вывода; не утверждается, что ошибка возникает в каждом интерактивном PowerShell.

Рабочий обход без изменения кода:

```powershell
.venv\Scripts\python -X utf8 scripts\demo.py
```

Рекомендуем другу заменить статусные эмодзи на `[OK]`/`[FAIL]` либо явно обеспечить UTF-8 и описать соответствующую команду. Файлы друга не изменялись.

### Живой OpenAI

Существующий ключ прочитан из окружения пользователя Windows только в память дочернего процесса. Модель: `gpt-5.4-mini`. `.env` не создавался; значение ключа не выводилось и в отчёт не включалось.

В браузере «Пример → Разобрать» заполнил Алматы, 15.10.2026, той, Ведущий, 1 000 000 ₸, казахский язык. Подбор после проверки формы вернул Кики, Сон Гоку и Эмилию с пометками «AI-объяснение».

Дополнительные реальные запросы `/parse`:

| Ввод | Получено | Время |
|---|---|---|
| Нужен фотограф в Алматы | Город и категория; дата, формат и бюджет пустые и перечислены как недостающие | 0.983 с |
| Ведущий, свадьба 15.10.2026, Алматы, 1 млн ₸, 4 часа, русский | Все указанные поля распознаны, missing пуст | 0.931 с |
| Ведущий, свадьба 01.01.2027, Алматы, 1 млн ₸ | Дата сброшена в null, выдано объяснение границ календаря | 0.910 с |

Независимо запущен существующий `scripts/eval.py`: 24 сценария, каждый дважды, кэш очищается. Отчёт сохранён отдельно; `docs/EVAL.md` друга не перезаписывался.

- 23 карточки в первых запусках; AI принят сразу **20/23**, после исправления **3/23**, шаблон **0/23**.
- Все 48 вызовов: среднее **1.002 с**, максимум **3.701 с**. В среднее входят пустые результаты без вызова LLM.
- Только 30 вызовов с непустой выдачей: среднее **1.604 с**.
- Порядок совпал **24/24**; ошибок вызовов **0**.
- Различные нормализованные тексты в **6/6** ответах с несколькими карточками.

Принятие объяснения означает прохождение валидатора приложения, не доказательство полной смысловой корректности. Скорость измерена для последовательных вызовов `match()` без загрузки CSV, не для параллельной нагрузки и не для полного времени браузера. Проверки отказа AI присутствуют в автоматических тестах; реальный сбой провайдера в этом запуске не возникал.

Подробные результаты: [LIVE_EVAL_2a45fc7.md](acceptance/LIVE_EVAL_2a45fc7.md) и [LIVE_EVAL_2a45fc7.json](acceptance/LIVE_EVAL_2a45fc7.json).

### Итог

Чистая установка и запуск приложения на этой Windows подтверждены. Живые разбор текста и объяснения подтверждены. Демо-скрипт требует исправления переносимости вывода либо явного UTF-8. Повторная браузерная приёмка и закрытие CP1251 описаны ниже. Размещение этой приёмки, PITCH и файлов eval согласовано Askhat через Nurdaulet.


## Повторная приёмка 8ca19e5 — дизайн c01517a и Windows CP1251

Проверено 23.09.2026 после обновления основной рабочей копии. Код приложения с 2a45fc7 не менялся; изменения затронули web/index.html, README, demo.py и его тест. Полный живой eval повторно не запускался: числовые показатели выше остаются результатами 2a45fc7.

- **202 passed, 1 warning in 3.19 s.** Первая попытка: 201 passed и одна ошибка подготовки tmp_path из-за WinError 5 в общей временной папке pytest. Повтор с новой отдельной папкой через `--basetemp` прошёл полностью. Права доступа и тесты не изменялись, тесты не пропускались. Это ограничение окружения проверяющего, а не падение утверждения теста. Предупреждение Starlette/AnyIO осталось.
- `.venv\Scripts\python scripts\demo.py` **без -X utf8**: 28 проверок прошли, код завершения 0; выводит [OK] вместо эмодзи в CP1251. Добавленный тест с PYTHONIOENCODING=cp1251 тоже прошёл. Прежний UnicodeEncodeError закрыт.
- Сервер запущен из основной папки; живой gpt-5.4-mini. «Пример → Разобрать» заполнил Алматы / 15.10.2026 / той / Ведущий / 1 млн ₸ / казахский. «Подобрать» вернул Кики, Сон Гоку, Эмилию с AI-объяснениями. Во время ожидания кнопки блокируются и отображается состояние загрузки.
- «Расчёт оценки» и «Проверка всех кандидатов» раскрываются. Для Кики отображаются компоненты, суммарно 70.5.
- Изменение только даты на 16.10.2026: пустой результат с причинами; в деталях Кики, Сон Гоку и Эмилия заняты на 16.10.2026. Это ожидаемо для «той + казахский», не регрессия дизайна.
- Алматы / 15.10.2026 / свадьба / Флорист / 500 000 ₸ / любой язык: Тихиро Огино и Тони Тони Чоппер, причина двух карточек, флаги синтетического профиля и проставленной цены видны.
- Астана / 15.10.2026 / свадьба / Декоратор / 1 млн ₸: категории нет, указан Алматы.
- Дата 01.01.2027 не проходит нативную валидацию формы: rangeOverflow=true, сообщение браузера «Максимальное значение должно быть 31.12.2026.». Текст системной подсказки зависит от браузера.
- Узкое окно 390×844: форма, результат, цены, флаги и объяснения видны; горизонтального переполнения нет, в том числе после раскрытия оценки. Выполнены действия с формой в узком окне. Затем размер окна восстановлен.

**Вердикт:** запрошенные сценарии дизайна пройдены, исправление Windows подтверждено. Это приёмка указанного коммита, не оценка ИИ-жюри и не гарантия поведения всех запросов. Полное семантическое ревью объяснений и нагрузочная проверка не выполнялись.

## Повторная приёмка d7f3ff6

Дата: 23.09.2026. Проверено независимо до 17:20. Рабочая копия обновлена fast-forward; код и документы репозитория проверяющим не изменялись.

### Подтверждено

- **204 passed, 1 warning in 4.48 s**. Использована новая отдельная временная папка pytest через --basetemp, чтобы избежать ранее обнаруженного конфликта прав общего Temp. Предупреждение — устаревающий интерфейс Starlette/AnyIO.
- Точная Windows-команда `.venv\Scripts\python scripts\demo.py` без -X utf8: **28/28**, код завершения 0, метки [OK] в CP1251.
- Повторён исходный независимый скрипт сетки: **1344 запроса**, **690 непустых**, **654 пустых**, **0 одинаковых объяснений** после удаления имён. В прежней версии было 8 запросов с совпадениями. Ошибок проверяемых фильтров, количества карточек и предложений, отсутствия объяснений малой/пустой выдачи не выявлено.
- Проблемный запрос: Алматы / 15.10.2026 / корпоратив / Лайв-бэнд / 4 млн ₸. Шаблоны теперь различаются конкретикой состава: два вокалиста и вокалистка с перечислением инструментов против четырёх вокалистов. Эти сведения присутствуют в полных исходных описаниях. Каталог не менялся.
- README: указаны 204 теста, основной вызов AI плюс возможное исправление, отдельный /parse; ограниченность проверки различимости описана явно.
- В браузере живой gpt-5.4-mini: «Пример → Разобрать → проверить поля → Подобрать» заполнил Алматы / 15.10.2026 / той / Ведущий / 1 млн ₸ / казахский и вернул Кики, Сон Гоку, Эмилию с AI-объяснениями. Форма и API работают.

Доказательство сетки: [JUDGE_PROBE_d7f3ff6.json](acceptance/JUDGE_PROBE_d7f3ff6.json) в docs/acceptance/. P1 в части воспроизведённых одинаковых шаблонов закрыт на проверенной сетке; P2 про число тестов и количество вызовов в README закрыт.

### Новое замечание: необоснованное объяснение относительного места

Дополнительно выполнен **один реальный HTTP-запрос с AI** для проблемной категории Лайв-бэнд. Ответ получен за 2.246 с, три текста различаются и приняты валидатором. Однако:

- Дзэнъицу Агацума: «Подрядчик выделяется самым широким составом…».
- Рей Аянами: «Подрядчик идёт следом за более насыщенным составом…».

У обоих score=5.5625 и одинаковая разбивка: язык 2, цена 3.5625, остальные компоненты 0. Порядок задаётся ID (HK-23752 перед HK-83709), а состав музыкантов вообще не входит в score. Утверждение о превосходстве состава не подтверждено переданными сравнительными данными. Полное описание Рей также содержит струнный квартет и другие инструменты; короткая цитата «4 вокалиста» не описывает весь состав.

Таким образом, **различные тексты не означают корректные причины ранжирования**. Это отдельная семантическая неточность живого AI; она не опровергает успешную проверку отсутствия одинаковых шаблонов.

Сырой реальный ответ сохранён в [LIVE_BANDS_d7f3ff6.json](acceptance/LIVE_BANDS_d7f3ff6.json), без ключа и конфигурации окружения. Модель вариативна: повтор не обязан дословно воспроизвести текст, поэтому для регрессионного теста нужен сохранённый пример ответа.

Рекомендуем разработчику: описывать, почему карточка подходит, и её конкретные факты; не требовать от модели объяснять каждое место в списке. Если объяснение места сохраняется, передавать рассчитанные различия score и признак равенства, не связывать порядок с неучтёнными признаками. Запретить неподтверждённые превосходные/сравнительные формулировки вроде «самый широкий состав». При равных score не утверждать преимущество одного кандидата; публично выводить ID для объяснения не требуется. Добавить проверку сохранённого ответа на равных баллах.

### Вердикт

Все запрошенные автоматические проверки и запуск прошли. Прежние P1/P2 закрыты в указанной области проверки. Перед финальной сдачей рекомендуется убрать неподтверждённые сравнительные причины из AI-объяснений. Полный eval 24×2 на новой версии не повторялся; прежние показатели остаются привязанными к 2a45fc7. Публикация этого исторического отчёта согласована; замечание закрыто в следующем разделе cd242e9.

## Повторная проверка cd242e9

23.09.2026, выполнено до 17:20. Код и документы репозитория не изменялись; публикация результатов согласована Askhat через Nurdaulet.

### Автоматические проверки

- **207 passed, 1 warning in 5.10 s**. Все тесты, включая регрессию сохранённого неподтверждённого сравнения, прошли. Использована новая отдельная папка --basetemp; предупреждение Starlette/AnyIO не связано с изменением.
- Windows: `.venv\Scripts\python scripts\demo.py` без -X utf8 — **28/28**, код завершения 0, метки [OK] при CP1251.
- Исходный независимый скрипт повторён без изменения сетки: **1344 запроса**, **690 непустых**, **654 пустых**, **0 совпадающих объяснений** после удаления имён. Не выявлено ошибок проверяемых фильтров, количества карточек/предложений и причин малой/пустой выдачи. Результат: [JUDGE_PROBE_cd242e9.json](acceptance/JUDGE_PROBE_cd242e9.json).
- README указывает 207 тестов и новую проверку сравнений при равной оценке. В коде score и score_tied передаются модели, сохранённые неудачные тексты отклоняются регрессионными тестами.

### Живой OpenAI: gpt-5.4-mini

Выполнены два реальных HTTP-запроса: Алматы / корпоратив / Лайв-бэнд / 4 млн ₸, даты 15.10.2026 и 23.09.2026. Сервер запущен заново на cd242e9, запросы разные, кэш ранее пуст.

| Дата | Время HTTP-ответа | Результат |
|---|---:|---|
| 15.10.2026 | 2.481 с | 3 карточки, все объяснения llm с первой попытки; у двух равный score=5.5625 и score_tied=true |
| 23.09.2026 | 4.007 с | 3 карточки с равным score=5.5625; 2 llm, 1 template после неудачного исправления похожего текста |

В проверенных ответах больше нет «самого широкого состава» и объяснения порядка превосходством состава. Пример новой фразы для Рей: «отдельно указан состав из 4 вокалистов». Для второго запроса у этой карточки честный шаблон с фактом «4 вокалиста», причина fallback — «Объяснение слишком похоже на другую карточку», explanation_attempts=2. Все итоговые тексты различаются.

Сырые ответы: [LIVE_TIED_cd242e9.json](acceptance/LIVE_TIED_cd242e9.json). Значения ключей не сохранялись.

В браузере также пройдено «Пример → Разобрать → проверить поля → Подобрать»: Алматы / 15.10.2026 / той / Ведущий / 1 млн ₸ / казахский. Получены Кики, Сон Гоку и Эмилия. Все три итоговых объяснения — AI; у Эмилии видна пометка «AI-объяснение (исправлено)» и оговорка о подтверждении проставленной цены. Форма, состояния ожидания и отображение результата работают.

### Ограничения вывода

Проверено устранение конкретного дефекта на сохранённых примерах и двух живых запросах. Лексический проверщик не является полной проверкой смысла; это не гарантия для любой возможной формулировки модели. Полный eval 24×2 на этом хеше не повторялся, его старые показатели относятся к 2a45fc7. Чистая установка ранее проверена на 2a45fc7, зависимости после неё не менялись.

**Вердикт:** прежние замечания по совпадающим шаблонам, README и воспроизведённым необоснованным сравнениям закрыты в проверенной области. Новых блокирующих дефектов в этих проверках не выявлено. Публикация этого отчёта согласована Askhat через Nurdaulet.

## Контроль замороженной версии 5bb978f — 23.09.2026

После апрува публикации отчётов репозиторий синхронизирован с указанной разработчиком замороженной версией **5bb978f**. Код не менялся проверяющим; этот коммит приёмки содержит только документы и доказательства. Новые живые запросы OpenAI на 5bb978f не выполнялись: приведённые выше результаты сохраняют исходные хеши d7f3ff6 и cd242e9. Финальный живой замер разработчика на 4c256b9 находится отдельно в [EVAL.md](EVAL.md).

- **210 passed, 1 warning in 5.07 s**, с новой отдельной папкой --basetemp. Предупреждение Starlette/AnyIO прежнее.
- Windows-команда `.venv\Scripts\python scripts\demo.py` без -X utf8: **28/28**, код завершения 0.
- Независимая неизменённая сетка: **1344 запроса, 690 непустых, 654 пустых, 0 совпадений**; нарушений дополнительно проверяемых фильтров, числа карточек/предложений и причин малой/пустой выдачи не выявлено. [JSON результата](acceptance/JUDGE_PROBE_5bb978f.json).
- Uvicorn запущен без ключа и модели. `/health`: HTTP 200, 66 профилей. Базовый HTTP `/match`: Алматы / 15.10.2026 / свадьба / Ведущий / 1 млн ₸ → Кики, Эмилия, Сон Гоку, все объяснения template.
- Секреты и пути к файлам учётных данных не включались в новые доказательства; перед коммитом выполнена проверка подготовленных файлов. Новые материалы публикуются по явному APPROVE пользователя.
- Точка доступа с телефона не использовалась. Настройки сети проверяющий не менял.

Эта контрольная проверка не является новой чистой установкой, полным живым eval или повторением всей браузерной приёмки. Зафиксированные ограничения лексического валидатора и проверки только ограниченной выборки сохраняются. Оснований открывать заново исправленные конкретные дефекты в выполненных проверках не выявлено.
